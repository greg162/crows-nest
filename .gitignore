# ---- .NET ----
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
*.suo
*.userosscache
*.sln.docstates
.vs/
artifacts/
TestResults/
*.trx
*.coverage
*.nupkg

# ---- Vendored native SimConnect (see spec s13, "vendor into the repo") ----
# Deliberately NOT ignored: lib/simconnect/**. Tracked on purpose.

# ---- ESP-IDF firmware ----
# Build output: hundreds of MB, fully regenerable.
firmware/**/build/
# Managed components: 201 MB for LVGL alone. Never commit. (spec s9.6 F5)
firmware/**/managed_components/
# Generated 2,325-line config. Only sdkconfig.defaults.* is tracked. (spec s9.6)
firmware/**/sdkconfig
firmware/**/sdkconfig.old
# Tracked on purpose, do not ignore: dependencies.lock (pins resolved versions for CI)

# ---- Editor / OS ----
.idea/
*.swp
Thumbs.db
desktop.ini
