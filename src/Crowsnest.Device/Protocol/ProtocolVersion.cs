namespace Crowsnest.Device.Protocol;

public static class ProtocolVersion
{
    /// <summary>Wire protocol version carried in every frame's "v" field (spec §6.1).</summary>
    public const int Current = 2;
}
